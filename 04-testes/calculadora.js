const findMultply = (a1, a2) => a1 * a2;
const findDivision = (b1, b2) => b1 / b2;
const findSoma = (c1, c2) => c1 + c2;
const findSub = (d1, d2) => d1 - d2;


module.exports = {
	findMultply,
	findDivision,
	findSoma,
	findSub	
}